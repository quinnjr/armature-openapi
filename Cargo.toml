[package]
name = "armature-openapi"
version = "0.3.0"
edition = "2024"
rust-version = "1.94.1"
authors = ["Joseph Quinn <quinn.josephr@proton.me>"]
license = "Apache-2.0"
repository = "https://github.com/quinnjr/armature-openapi"
homepage = "https://quinnjr.github.io/armature"
description = "OpenAPI specification generation for Armature APIs"
keywords = ["openapi", "swagger", "api", "documentation"]
categories = ["web-programming", "api-bindings"]

[dependencies]
armature-core = { version = "0.9" }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"

# Async
tokio = { version = "1.52", features = ["rt", "rt-multi-thread", "macros", "sync", "time"] }
async-trait = "0.1"

# Utilities
thiserror = "2.0"

[dev-dependencies]
cargo-husky = { version = "1.5", default-features = false, features = ["user-hooks"] }
tokio-test = "0.4"


